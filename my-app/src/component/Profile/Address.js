import React from "react";

const Address = ({ Address }) => {
    return (
        <div>
            <p style={{ color: "red", fontSize: "25px" }}>{Address}</p>
        </div>
    );
};

export default Address;